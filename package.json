{
  "name": "runny-devtools",
  "private": true,
  "description": "Dev-only: commitlint for the commit-msg hook. Not part of the build graph.",
  "devDependencies": {
    "@commitlint/cli": "21.1.0",
    "@commitlint/config-conventional": "21.1.0"
  }
}
